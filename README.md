# Advanced_ML_project_1

Project 1 for Advanced Machine Learning:
logistic regression with missing labels, FISTA logistic lasso, and UnlabeledLogReg.

## Current status
The repository is being refactored from a notebook-based prototype into a modular Python project.

## Planned structure
- `notebooks/` – archived prototype and exploration
- `src/project1/` – source code
- `tests/` – tests
- `scripts/` – runnable scripts
- `data/` – datasets
- `outputs/` – generated figures, tables, logs
